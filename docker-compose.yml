services:
  web:
    build: .
    image: pbskydiving:latest
    restart: unless-stopped
    expose:
      - "80"
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
